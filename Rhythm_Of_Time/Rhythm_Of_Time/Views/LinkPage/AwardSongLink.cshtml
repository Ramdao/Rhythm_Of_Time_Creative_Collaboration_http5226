@model AwardLinkDetails

@{
    ViewData["Title"] = "Song Artist Management";
}

<h1>@ViewData["Title"]</h1>

<!-- List of Songs -->
<h3>Users</h3>
<a asp-controller="AdminPage" asp-action="List">Back to Users</a>
<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Album</th>
            <th>Year</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var song in Model.Songs)
        {
            <tr>
                <td>@song.Title</td>
                <td>@song.Album</td>
                <td>@song.ReleaseYear</td>
                <td>
                    <!-- Link Form -->
                    <form method="post" action="@Url.Action("LinkAwardToSong", "LinkPage")" style="display:inline;">
                        <input type="hidden" name="SongId" value="@song.SongId" />

                        <!-- Dropdown to select timeline -->
                        <select name="ArtistId" class="form-control" required>
                            <option value="">Select Artist</option>
                            @foreach (var timeline in Model.Awards)
                            {
                                <option value="@timeline.AwardId">@timeline.name</option>
                            }
                        </select>

                        <!-- Description Field -->
                        <input type="text" name="Description" class="form-control mt-2" placeholder="Enter Description" required />

                        <button type="submit" class="btn btn-success mt-2">Link</button>
                    </form>

                    <!-- Unlink Form -->
                    <form method="post" action="@Url.Action("UnLinkAwardToSong", "LinkPage")" style="display:inline;">
                        <input type="hidden" name="SongId" value="@song.SongId" />

                        <!-- Dropdown to select timeline -->
                        <select name="ArtistId" class="form-control" required>
                            <option value="">Select Award</option>
                            @foreach (var timeline in Model.Awards)
                            {
                                <option value="@timeline.AwardId">@timeline.name</option>
                            }
                        </select>

                        <button type="submit" class="btn btn-danger mt-2">Unlink</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
